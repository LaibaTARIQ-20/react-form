import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#FF5722'
    },
    background: {
      default: '#1a1a1a'
    },
    secondary: {
      main: '#E64A19'
    },
    Text: {
      primary: '#ffffff'
    }

  }
});
export default theme;